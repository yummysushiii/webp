const changeText = (event) => {
    console.log(event.target)
    event.target.innerText = event.target.innerText + "被點了"
}

const account=(num)=>{ 
    var output = <input type = "text"></input>;

    return output;
}
  
export default account;